<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ponfee.web.framework.dao.mapper.RegisterContentionMapper">

  <insert id="insert">
    INSERT INTO t_register_contention (attr, ckey, cval)
    VALUES (#{attr},#{ckey},#{cval})
  </insert>

  <select id="get" resultType="string">
    SELECT cval FROM t_register_contention 
    WHERE attr=#{attr} AND ckey=#{ckey}
  </select>

</mapper>
